% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_required.R
\name{conditional_required}
\alias{conditional_required}
\title{Conditional Required Validator}
\usage{
conditional_required(condition, field)
}
\arguments{
\item{condition}{Function returning a logical condition.}

\item{field}{Field that becomes required.}
}
\value{
Returns a validation function used inside
\code{\link{add_validation}}.
}
\description{
Flags missing values when a condition is met.
}
\details{
Useful for:
\itemize{
\item required phone numbers for active participants
\item mandatory exit dates for exited beneficiaries
\item school details for enrolled children
\item viral load dates for CALHIV records
}

The validator flags rows where:

\itemize{
\item the condition evaluates to TRUE
\item and the required field is missing
}
}
\examples{

library(validationcheck)
library(magrittr)

# initialize validation agent ----------------------------------------

report <- validate_data(sample_registry)

# example 1: exit date required for exited participants --------------

report <- report \%>\%

  add_validation(

    label = "Missing Exit Date",

    columns = c(
      "program_status",
      "date_exited"
    ),

    rule =
      "Exited participants must have an exit date",

    check = conditional_required(

      condition = function(data){
        data$program_status == "EXITED"
      },

      field = "date_exited"
    )
  )

# example 2: school name required for enrolled children --------------

report <- report \%>\%

  add_validation(

    label = "Missing School Name",

    columns = c(
      "school_status",
      "school_name"
    ),

    rule =
      "Children enrolled in school must have a school name",

    check = conditional_required(

      condition = function(data){
        data$school_status == "IN SCHOOL"
      },

      field = "school_name"
    )
  )

# generate report ----------------------------------------------------

get_report(report)

}
\seealso{
\code{\link{add_validation}}
\code{\link{validate_data}}

Other validators: 
\code{\link{after_date}()},
\code{\link{before_date}()},
\code{\link{between_dates}()},
\code{\link{compare_columns}()},
\code{\link{contains}()},
\code{\link{date_before}()},
\code{\link{ends_with}()},
\code{\link{equal_to}()},
\code{\link{exclude}()},
\code{\link{future_date}()},
\code{\link{greater_equal}()},
\code{\link{greater_than}()},
\code{\link{include}()},
\code{\link{is_negative}()},
\code{\link{is_positive}()},
\code{\link{less_equal}()},
\code{\link{less_than}()},
\code{\link{matches_regex}()},
\code{\link{not_equal}()},
\code{\link{past_date}()},
\code{\link{range_between}()},
\code{\link{starts_with}()}
}
\concept{validators}
